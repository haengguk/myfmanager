package com.lolfm.simulator;

/** Structured result of a deterministic Jungle Tempo eligibility check. */
public enum JungleTempoReadinessStatus {
    READY,
    NO_CURRENT_ECONOMY_OUTCOME,
    INSUFFICIENT_CREDIT
}
