package com.lolfm.composition;

/** Structured comparison actually executed by the runtime. */
public enum CompositionRuntimeComparisonOperator {
    SAMPLE_LESS_THAN_PROBABILITY,
    NOISY_SCORE_GREATER_THAN_OR_EQUAL_ZERO
}
