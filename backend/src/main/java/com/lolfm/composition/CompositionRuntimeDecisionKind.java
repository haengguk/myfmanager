package com.lolfm.composition;

/** Structured identity of the runtime winner-selection algorithm. */
public enum CompositionRuntimeDecisionKind {
    WEIGHTED_SELECTION,
    UNIFORM_NOISE_THRESHOLD
}
