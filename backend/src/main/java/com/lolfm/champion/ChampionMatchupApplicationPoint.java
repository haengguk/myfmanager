package com.lolfm.champion;

/** Runtime consumer that actually receives a Matchup contribution. */
public enum ChampionMatchupApplicationPoint {
    LANE_OPPORTUNITY_PRESSURE,
    COMBAT_PROGRESSION_SCORE
}
