import type { LeagueFixtureViewDto } from './api/leagueApi.types';
import { FIXTURE_STATUS_COPY } from './league.adapter';

export function LeagueScheduleTable({ fixtures, selectedId, onSelect }: { fixtures: readonly LeagueFixtureViewDto[]; selectedId: string | null; onSelect: (fixture: LeagueFixtureViewDto) => void }) {
  return <div className="lg-schedule-table" role="region" aria-label="리그 경기 일정" tabIndex={0}><table><thead><tr><th scope="col">R</th><th scope="col">대진</th><th scope="col">모드</th><th scope="col">상태</th><th scope="col">Game 1 진영</th><th scope="col">결과</th></tr></thead><tbody>{fixtures.map((fixture) => <tr key={fixture.fixtureId} className={selectedId === fixture.fixtureId ? 'is-selected' : ''} onClick={() => onSelect(fixture)}><td>{String(fixture.roundNumber).padStart(2, '0')}</td><th scope="row"><button type="button" onClick={() => onSelect(fixture)} aria-pressed={selectedId === fixture.fixtureId}><b>{fixture.firstTeamCode}</b><i>vs</i><b>{fixture.secondTeamCode}</b></button></th><td><span className={`lg-mode is-${fixture.executionMode === 'FULL_AUTO' ? 'auto' : 'player'}`}>{fixture.executionMode === 'FULL_AUTO' ? 'AUTO' : 'PLAYER'}</span></td><td><span className={`lg-status-dot is-${fixture.lifecycleStatus.toLowerCase()}`} />{FIXTURE_STATUS_COPY[fixture.lifecycleStatus]}</td><td>{fixture.game1BlueTeamCode} <i>/</i> {fixture.game1RedTeamCode}</td><td>{fixture.lifecycleStatus === 'COMPLETED' ? '반영 완료' : '—'}</td></tr>)}</tbody></table></div>;
}
